// Courier tools have been removed from Productivity MCP.
// Courier now has its own dedicated MCP server at:
// https://email-bot-server.micaiah-tasks.workers.dev/sse
//
// This file is kept as a placeholder to prevent import errors during transition.
// It can be safely deleted once the transition is complete.

import type { ToolContext } from '../types';

export function registerCourierTools(ctx: ToolContext) {
  // No-op - Courier tools now live in dedicated MCP server
}
